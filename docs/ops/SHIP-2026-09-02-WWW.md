# Ship pack — trydemigod.com — 2026-09-02

Prepared this session. Overlay proofs passed locally. This file is the operator paste; it does not Designer-publish by itself.

## Live now

| | Value |
|---|---|
| Last Webflow publish | Thu Aug 27 2026 17:05:18 GMT |
| Foot pin | `3b0761f2eb93641bd60b90945429b96b4b847413` (v1109) |
| `/pricing` source | `<h4 class="heading_secondary margin-bottom_none">$0</h4>` |
| GitHub `main` | `7157dcc` (v1109 era) |
| Local overlay | **v1122** `foot-latest.js` = `foot-v1122.js`, sha256 `d7a00da5c0da` |

Local proofs (this session):

- `ruby scripts/proof-release-version.rb` — ok v1122
- `ruby scripts/proof-pricing-h4.rb` — `li.card` h4 `$0` → `10%`
- `ruby scripts/proof-static-hide.rb` — `#dg-static` hidden when JS runs
- `ruby scripts/proof-identity.rb` — Demigod matching desk; no DIE in overlay; no `$DASHA`

Do not `git push` the whole local `main`. Unpushed history includes KYC/grant packets. Ship **only** overlay bytes.

## GitHub (jsDelivr)

Branch opened: `ship-v1122-overlay` from `7157dcc`. Files to land on that branch (not Stripe, not KYC):

- `foot-latest.js`
- `foot-v1122.js` (byte-identical)
- `head-latest.css`
- `startup-map-latest.js`

This Mac cannot `git push` (HTTPS prompts disabled, no `gh`). After those four files exist on GitHub, substitute **that commit SHA** for `<COMMIT-SHA>` below. jsDelivr caches commit pins forever — never reuse `3b0761f2…`.

SRI (local v1122 bytes):

- foot `sha384-UNMVAHEl5Fv0Un0TNGaRQxNaIkRoCEADgo0KPOJYXrDXKg5YnP3dMSNeq036vsLP`
- head `sha384-H9+pk3D3kyTEes2uPsHyqbiwwElWaC0Bra4SKdCPorjfh+oSqw8+rC0hS6El4zc2`

## Webflow paste (one SHA, every 200 route)

`/how` `/pricing` `/faq` `/hire` `/talent` (and any alias kept as 200). `/faq` has historically dropped to a lone loader — audit it.

```html
<link rel="preload" as="script" href="https://cdn.jsdelivr.net/gh/Uuriko/demigod-site-cdn@<COMMIT-SHA>/foot-latest.js" data-dg-foot-preload>
<meta name="dg-startup-map-script" content="https://cdn.jsdelivr.net/gh/Uuriko/demigod-site-cdn@<COMMIT-SHA>/startup-map-latest.js">
<meta name="dg-startup-map-data" content="https://cdn.jsdelivr.net/gh/Uuriko/demigod-site-cdn@<COMMIT-SHA>/sf-startup-map.json">
<meta name="dg-startup-roles-feed" content="https://cdn.jsdelivr.net/gh/Uuriko/demigod-site-cdn@<COMMIT-SHA>/roles-feed.json">
<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/Uuriko/demigod-site-cdn@<COMMIT-SHA>/head-latest.css" integrity="sha384-H9+pk3D3kyTEes2uPsHyqbiwwElWaC0Bra4SKdCPorjfh+oSqw8+rC0hS6El4zc2" crossorigin="anonymous">
<script id="demigod-foot-cdn-loader" src="https://cdn.jsdelivr.net/gh/Uuriko/demigod-site-cdn@<COMMIT-SHA>/foot-latest.js" integrity="sha384-UNMVAHEl5Fv0Un0TNGaRQxNaIkRoCEADgo0KPOJYXrDXKg5YnP3dMSNeq036vsLP" crossorigin="anonymous" defer></script>
```

Then Webflow queue-publish. Gate is `taskStatus: finished`, not the click.

Noscripts (crawlers): `docs/ops/static-html/noscript-{how,pricing,faq,hire,talent}.html`.

Aliases `/compare` `/pilot` `/network`: true 308 or full noscript + head canonical.

## Verify after publish

```sh
ruby scripts/verify-live-release.rb --expected-sha=<COMMIT-SHA> --expected-version=1122
```

Raw Webflow HTML may still contain `$0` before overlay JS. Post-JS heading must be **10%**.

## Do not

- Designer-publish without the six-tag pin in the same change set
- Pin `3b0761f2eb93641bd60b90945429b96b4b847413` or `d3cce0d74a24ba5d2bacb984e710dcb27e260d3e`
- Put DIE, EIN, or Dasha on overlay
- wrangler-deploy from this repo
