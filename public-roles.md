# Demigod public-roles overlay

`public-roles.json` is the curated overlay the site reads as `demigod.public-roles/1` (`window.__dgPublicRoles`).

This file is **X-week 2026-08-14 from @nazzari**, not ATS first-observation. Do not merge these rows into `roles-feed.json` as if Demigod saw them on Greenhouse, Ashby, or Lever.

Source: [jobs on X this week / weekly x job feed 08/14/26](https://x.com/nazzari/status/2088312787915399337)

- Tweet-only rows use `provider: "X"` and the tweet as `url`.
- xAI and Auth0 Lab (Okta) use a real employer board URL; `sourceTweet` stays on the row.
- `firstObservedAt` is 2026-08-14 (overlay date). `postedAt` is null — we do not have an employer date.
